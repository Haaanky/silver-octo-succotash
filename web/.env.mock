# Placeholder credentials used when running local mock tests (npm run test:mock).
# All Supabase API calls are intercepted by Playwright – these values are never
# sent over the network.
VITE_SUPABASE_URL=https://ihqqqynuqclycffgraxl.supabase.co
VITE_SUPABASE_ANON_KEY=mock-anon-key-local-testing
